"""
Oftentimes over the course of the analysis we only will want data for certain years. 
For example, we may want data from before and after a policy is put in place.
This function selects a subset of data for various year inputs. 

"""
import os
import pandas as pd 

##Changing directory to appropriate location. 

os.chdir("C:/Users/Felipe/Desktop/Duke MIDS/Practical Tools in Data Science/")

#Import data on shipments- Using subset of data as an example. 
shipments = pd.read_excel("Copy of SubsetRows.xlsx")

def year_subset(df, start, end) : 

    boolean_years = (df['YEAR'] > start) & (df['YEAR'] <= end)
    filtered_data = df[boolean_years]
    return(filtered_data)